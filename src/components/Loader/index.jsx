import React from 'react';
import {MainContainer, Loading } from './Loader.styles';

export const Loader = () => {
    return (
        <MainContainer>
            <Loading />
        </MainContainer>
    )
}
